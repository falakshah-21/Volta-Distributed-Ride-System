using Microsoft.AspNetCore.SignalR;

namespace Volta.Services.Ride.Hubs
{
    // This class handles the open phone lines
    public class NotificationHub : Hub
    {
        // We can add methods here if the phone wants to talk back to the server.
        // For now, we leave it empty because we only want Server -> Phone.
    }
}